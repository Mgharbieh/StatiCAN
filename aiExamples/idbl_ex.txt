arduino_mcp2515 examples:

SOURCE CODE PROBLEM SNIPPET:
[
  void setup() {
  canMsg1.can_id  = 0x0F6 | CAN_EFF_FLAG;
  canMsg1.can_dlc = 8;
  canMsg1.data[0] = 0x8E;
  canMsg1.data[1] = 0x87;
  canMsg1.data[2] = 0x32;
  canMsg1.data[3] = 0xFA;
  canMsg1.data[4] = 0x26;
  canMsg1.data[5] = 0x8E;
  canMsg1.data[6] = 0xBE;
  canMsg1.data[7] = 0x86;

  canMsg2.can_id  = 0x12345678;
  canMsg2.can_dlc = 8;
  canMsg2.data[0] = 0x8E;
  canMsg2.data[1] = 0x87;
  canMsg2.data[2] = 0x32;
  canMsg2.data[3] = 0xFA;
  canMsg2.data[4] = 0x26;
  canMsg2.data[5] = 0x8E;
  canMsg2.data[6] = 0xBE;
  canMsg2.data[7] = 0x86;

  canMsg3.can_id  = 0xFFF | CAN_SFF_FLAG;
  canMsg3.can_dlc = 8;
  canMsg3.data[0] = 0x8E;
  canMsg3.data[1] = 0x87;
  canMsg3.data[2] = 0x32;
  canMsg3.data[3] = 0xFA;
  canMsg3.data[4] = 0x26;
  canMsg3.data[5] = 0x8E;
  canMsg3.data[6] = 0xBE;
  canMsg3.data[7] = 0x86;
}

]

OUTPUTS FOR THIS EXAMPLE:
[
A(n) standard ID Bit Length is set during frame initialization for 0x0F6 but uses a(n) extended flag when sending message.
A(n) extended ID Bit Length is set during frame initialization for 0x12345678 but uses a(n) standard flag when sending message.
A(n) extended ID Bit Length is set during frame initialization for 0xFFF but uses a(n) standard flag when sending message.
]

SOLUTIONS FOR THIS EXAMPLE:
[
    Issue Type: ID Bit Length
    Issue Number: 1
    Issue Messages: A(n) standard ID Bit Length is set during frame initialization for 0x0F6 but uses a(n) extended flag when sending message. 
    Solution: A standard length ID is assigned to 'canMsg1' yet a 'CAN_EFF_FLAG' is flagged indicating an extended frame. If this was not the intention, remove the 'CAN_EFF_FLAG'

    Issue Type: ID Bit Length
    Issue Number: 2
    Issue Messages: A(n) extended ID Bit Length is set during frame initialization for 0x12345678 but uses a(n) standard flag when sending message.
    Solution: An extended length ID is assigned to 'canMsg2' yet it is missing a 'CAN_EFF_FLAG' flag to indicate an extended frame. In this case, because you do not utilize the flag, a standard ID length is expected. Change 'canMsg2.can_id  = 0x12345678;' to 'canMsg2.can_id  = 0x12345678 | CAN_EFF_FLAG;'

    Issue Type: ID Bit Length
    Issue Number: 3
    Issue Messages: A(n) extended ID Bit Length is set during frame initialization for 0xFFF but uses a(n) standard flag when sending message.
    Solution: An extended length ID is assigned to 'canMsg3' yet it is missing a 'CAN_EFF_FLAG' flag to indicate an extended frame. Change 'canMsg3.can_id  = 0xFFF;' to 'canMsg3.can_id  = 0xFFF | CAN_EFF_FLAG;'

]